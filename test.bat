echo "${WORKSPACE}"
echo "This is a message from a batch file"
${WORKSPACE}
%WORKSPACE%
echo %WORKSPACE%
${env.WORKSPACE}
echo ${env.WORKSPACE}
%env.WORKSPACE%
echo %env.WORKSPACE%
$WORKSPACE
echo $WORKSPACE
